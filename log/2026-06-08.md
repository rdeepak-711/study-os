# 2026-06-08

**12:05** (1 session)

Day 1. Built a FastAPI + MySQL slice from a blank main.py, no AI. POST inserts a row, GET reads it back, both work.

MySQL connector syntax was completely new to me. MySQL connector->mydb, mydb.cursor->cursor, using cursor execute sql statements to create db(gate_slice), table(entries). For adding entries execute(sql, val), for fetching entries execute(sql) + cursor.fetchall() returns the rows

The FastAPI routes and the connect came back from memory fine. Where I froze was the raw SQL and the mysql-connector API. Three walls: a DB name with a hyphen blew up because MySQL read gate-slice as subtraction, so I renamed it gate_slice. execute() rejected my params because (text) is not a tuple, it needed the comma or a list [text]. And my GET printed the rows instead of returning them, so the client got null until I added return.

So the freeze is real but it is syntax recall, not logic. Looked it all up in docs, kept Claude off the code. One rep down.

---
